# VPS 部署记录

- Host: 207.57.128.201
- SSH: port 12250 / root
- App: `/opt/node-hunter`
- Service: `systemctl status node-hunter`
- 反代域名: **https://node.galiais.com**（Cloudflare + HTTPS）
- 源站: `http://127.0.0.1:8080` / `0.0.0.0:8080`
- Token: `b959439e8c8abe35a604fc2780192994`（`/opt/node-hunter/.env`）
- Schedule: every 30 min full (skip_ai), `max_test: 6000` + dial after_quality Top60
- Dial: sing-box 1.11.7 at `/opt/node-hunter/bin/sing-box`
  - `after_quality: true`, `max_nodes: 100`, `after_quality_max: 60`
  - candidate selection: diversified (country/protocol, de-prioritize CDN fake-alive)
- PUBLIC_URL: `https://node.galiais.com`
- Binary: `/opt/node-hunter/bin/node-hunter-server` (version `2.1.1-dial`, commit `dial-diversify`)
- Note: when `verified >= 10`, `/sub` publishes verified pool first (smaller, real-working)

## 订阅（推荐用域名）

- Base64: https://node.galiais.com/sub/base64?token=b959439e8c8abe35a604fc2780192994
- Clash:  https://node.galiais.com/sub/clash?token=b959439e8c8abe35a604fc2780192994
- Raw:    https://node.galiais.com/sub/raw?token=b959439e8c8abe35a604fc2780192994
- 索引:   https://node.galiais.com/sub?token=b959439e8c8abe35a604fc2780192994
- 按国家: 追加 `&country=US` / `DE` / `NL` / `JP` ...

## 运维

```bash
ssh -p 12250 root@207.57.128.201
systemctl status node-hunter
journalctl -u node-hunter -f
curl -X POST http://127.0.0.1:8080/api/jobs/full \
  -H 'Content-Type: application/json' \
  -d '{"skip_ai":true,"max_test":1200,"rounds":2}'
```
